[project]
name = "mujoco-bittle"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "gymnasium>=1.2.2",
    "mujoco>=3.4.0",
    "ruff>=0.14.8",
    "sbx-rl>=0.19.0",
    "tensorboard>=2.20.0",
]

# Note: SBX uses JAX which has better GPU support across platforms
# JAX will automatically detect and use available GPUs
